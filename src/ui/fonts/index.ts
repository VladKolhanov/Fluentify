import { Bree_Serif, Lato } from "next/font/google";

export const breeSerif = Bree_Serif({
  variable: "--font-bree",
  weight: ["400"],
  subsets: ["latin"],
});

export const lato = Lato({
  variable: "--font-lato",
  weight: ["400"],
  subsets: ["latin"],
});
